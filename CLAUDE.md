# Project purpose

kmux manages tmuxinator session configs, each with separate `KUBECONFIG` for conflict-free operations.  

# Critical Rules

- Verify before done: never mark a task complete without proving it works (run tests, demonstrate correctness)
